# Инструкция по настройке и запуску

## Быстрый старт

### 1. Установка зависимостей

```bash
bun install
```

### 2. Настройка базы данных

База данных PostgreSQL уже создана и сконфигурирована:
- **База данных:** `trans`
- **Хост:** `localhost:5432`
- **Пользователь:** `postgres`
- **Пароль:** `3141`

Миграции уже применены. Если нужно применить заново:

```bash
cd shared
bunx prisma migrate dev
bunx prisma generate
```

### 3. Настройка Telegram бота

Получите токен бота у [@BotFather](https://t.me/botfather) и обновите файл `bot/.env`:

```env
BOT_TOKEN=ваш_токен_сюда
DATABASE_URL="postgresql://postgres:3141@localhost:5432/trans?schema=public"
TELEGRAM_CHANNEL_URL=https://t.me/ваш_канал
TECH_SUPPORT_BOT=@ваш_бот_поддержки
```

### 4. Запуск проектов

**Откройте 3 терминала:**

#### Терминал 1: API сервер
```bash
cd shared/api
bun run dev
```
API будет доступно на `http://localhost:3001`

#### Терминал 2: Telegram бот
```bash
cd bot
bun run dev
```

#### Терминал 3: Админ-панель
```bash
cd admin
bun run dev
```
Админка будет доступна на `http://localhost:3000`

## Структура проекта

```
trans-proj/
├── bot/                    # Telegram бот
├── admin/                  # React админ-панель
├── shared/                 # Общая схема БД (Prisma)
│   ├── prisma/
│   └── api/                # Express API для админки
```

## Работа с базой данных

### Prisma Studio (GUI для БД)
```bash
cd shared
bunx prisma studio
```
Откроется на `http://localhost:5555`

### Создание новой миграции
```bash
cd shared
bunx prisma migrate dev --name название_миграции
```

### Сброс базы данных
```bash
cd shared
bunx prisma migrate reset
```

## Функционал бота

### Команды
- `/start` - Приветствие и главное меню
- `/menu` - Показать главное меню
- `/help` - Справка по командам
- `/add_note [номер] [текст]` - Добавить заметку в дневник

### Кнопки меню
- **📢 Канал** - Ссылка на Telegram канал
- **⚙️ Настройки** - Изменение имени, пароля, временной зоны
- **💎 Подписка** - Активация и управление подпиской
  - ✅ Активировать подписку (доступна после 4 дней использования)
  - 🎁 Подарить подписку
- **🆘 Техподдержка** - Ссылка на бота техподдержки
- **📔 Дневник наблюдений** - Заметки по принципам Трансерфинга (1-78)

### Дневник наблюдений
Пользователь может:
1. Добавлять заметки: `/add_note 5 Текст заметки`
2. Просмотр заметок: отправить номер принципа (1-78)
3. Все заметки сохраняются в БД

### Подписка
- Триал-период: 4 дня использования бота
- После 4 дней можно активировать подписку
- Триал-дни автоматически отслеживаются

## Админ-панель

### Разделы

#### 1. Дашборд
Статистика:
- Всего пользователей
- Активные подписки
- Записей в дневнике
- Пользователей с дневником

#### 2. Пользователи
- Список всех пользователей
- Детальная информация о каждом пользователе
- История записей в дневнике

#### 3. Подписки
- Список всех подписок
- Статус подписки (активна/неактивна)
- Триал-дни использования
- Даты активации и окончания

#### 4. Сообщения
Управление репликами бота:
- Создание новых сообщений
- Редактирование существующих
- Удаление сообщений
- Категоризация (общие, меню, сценарии)

#### 5. Настройки
Глобальные настройки системы:
- Ключ-значение пары
- Описание настроек
- Редактирование и удаление

## API Endpoints

### Пользователи
- `GET /api/users` - Список пользователей
- `GET /api/users/:id` - Детали пользователя
- `PUT /api/users/:id` - Обновить пользователя
- `DELETE /api/users/:id` - Удалить пользователя

### Подписки
- `GET /api/subscriptions` - Список подписок
- `PUT /api/subscriptions/:userId` - Обновить подписку

### Сообщения бота
- `GET /api/messages` - Список сообщений
- `GET /api/messages/:key` - Получить сообщение
- `POST /api/messages` - Создать/обновить сообщение
- `PUT /api/messages/:key` - Обновить сообщение
- `DELETE /api/messages/:key` - Удалить сообщение

### Настройки
- `GET /api/settings` - Список настроек
- `GET /api/settings/:key` - Получить настройку
- `POST /api/settings` - Создать/обновить настройку
- `PUT /api/settings/:key` - Обновить настройку
- `DELETE /api/settings/:key` - Удалить настройку

### Дневник
- `GET /api/diary` - Записи дневника (фильтр по userId, principle)
- `GET /api/diary/stats` - Статистика по дневнику

## Таблицы БД

### users
Основная таблица пользователей:
- `telegramId` - ID в Telegram (уникальный)
- `username, firstName, lastName` - Данные из Telegram
- `name` - Имя для настроек
- `password` - Пароль для настроек
- `timezone` - Временная зона

### subscriptions
Подписки пользователей:
- `userId` - Связь с пользователем
- `isActive` - Активна ли подписка
- `trialDaysUsed` - Использовано триал-дней (0-4)
- `activatedAt, expiresAt` - Даты активации и окончания

### diary_entries
Записи дневника наблюдений:
- `userId` - Связь с пользователем
- `principle` - Номер принципа (1-78)
- `note` - Текст заметки
- `createdAt` - Дата создания

### bot_messages
Реплики бота:
- `key` - Уникальный ключ (например, "welcome_message")
- `text` - Текст сообщения
- `category` - Категория (general, menu, introduction, daily, subscription)
- `description` - Описание для админки

### settings
Глобальные настройки:
- `key` - Уникальный ключ
- `value` - Значение
- `description` - Описание

### introduction_data
Данные вводного сценария:
- `userId` - Связь с пользователем
- `data` - JSON с данными
- `completed` - Завершён ли сценарий

## Следующие шаги развития

- [ ] Реализовать вводный сценарий при первом запуске
- [ ] Добавить утренние/вечерние сценарии с учётом timezone
- [ ] Реализовать обработчики изменения настроек (имя, пароль, время)
- [ ] Добавить автоматическое увеличение trialDaysUsed при использовании
- [ ] Добавить систему отправки уведомлений по расписанию
- [ ] Реализовать интеграцию с ботом техподдержки
- [ ] Добавить аутентификацию в админ-панели
- [ ] Добавить экспорт данных пользователей

## Возможные проблемы

### Бот не запускается
- Проверьте токен в `bot/.env`
- Убедитесь, что БД доступна

### Админка не подключается к API
- Убедитесь, что API запущено на порту 3001
- Проверьте настройки proxy в `admin/vite.config.ts`

### Ошибки БД
- Проверьте, что PostgreSQL запущен
- Проверьте подключение: `psql -U postgres -d trans`
- Примените миграции: `cd shared && bunx prisma migrate dev`

## Контакты

Для вопросов и предложений создавайте issues в репозитории.

